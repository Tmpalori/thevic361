{
  "name": "thevic361-admin-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Smoke tests for the static admin panel under docs/",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "vitest": "^1.6.0"
  }
}
