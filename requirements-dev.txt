# Dev / CI dependencies for The Vic 361 Python tests.
# Production runtime deps live in requirements.txt; this file is for the
# pytest suite that exercises collect_events.py + discover_venues.py.
-r requirements.txt
pytest>=8.0
