# ─── The Vic 361 — Weekly Event Collection ───────────────────────────────────
# Runs every Sunday evening to populate candidates.json for the admin to
# screen. Does NOT auto-publish: the live source of truth on Railway is the
# `published_events` row written by the admin Save & Publish flow, and
# docs/events.json in this repo is a curated fallback that Save & Publish
# (via GITHUB_TOKEN) is the only writer of. The weekly run uses
# --candidates-only so CI never overwrites that fallback with raw scraper
# output. See README.md "Public Event Submissions" / RAILWAY.md for details.
#
# Schedule note (DST drift): GitHub Actions cron runs in UTC and does NOT
# observe US daylight-saving transitions. The fixed cron below means the
# job fires:
#   - 6:00 PM Central during CDT (Mar–Nov, UTC-5)
#   - 5:00 PM Central during CST (Nov–Mar, UTC-6)
# The 1-hour shift twice a year is intentional and acceptable — the digest
# job and the admin's Sunday-evening review window both have plenty of
# slack. If you need an exact 6:00 PM Central year-round, switch to two
# scheduled triggers gated by month, or move the trigger to a service that
# honors timezones (e.g. Railway cron). Do NOT silently "fix" the cron each
# DST switch — that's an unsignalled drift the audit flagged.
# Also runs on manual trigger (workflow_dispatch).
#
# Timeout budget (job-level 25 min, with per-step caps):
#   - The Apr-29 manual run was killed at the previous 10-min cap after a
#     single 4-minute Apify discovery timeout left the collector with too
#     little budget to finish. The healthy Apr-26 scheduled run took 8m16s,
#     i.e. there was effectively no slack at all.
#   - Apify and Perplexity each use a 240s per-call timeout, and the
#     collector chains several of them; 25 minutes gives room for a couple
#     of those to time out without killing the whole run.
#   - Per-step timeouts cap each phase so a single hung dependency can't
#     swallow the entire budget — the failed step gets a clean "timed out"
#     annotation instead of the whole job being cancelled mid-write.
#   - PYTHONUNBUFFERED=1 + `python -u` flush stdout line-by-line so that if
#     a step does stall, the workflow log shows where it got stuck instead
#     of the silent gap we saw in run 25121385371.
#   - Discovery (Google Maps via Apify) has both an internal time budget
#     in the script (~220s, exits with partial results) AND
#     `continue-on-error: true` on the step. Run 25123919466 had per-
#     category Apify discovery hit the 6-min step timeout after 5/8
#     categories completed, killing the workflow before the collector
#     ran. Discovery is best-effort — the collector falls back to the
#     existing venues.json — so it must never block the weekly pull.
# ─────────────────────────────────────────────────────────────────────────────

name: Weekly Collect

on:
  schedule:
    # 23:00 UTC every Sunday. = 6 PM CDT (Mar–Nov) / 5 PM CST (Nov–Mar).
    # See header comment for why we accept this 1-hour DST drift.
    - cron: '0 23 * * 0'
  workflow_dispatch:       # Manual trigger from GitHub UI

permissions:
  contents: write          # Needed to push updated candidates.json + venue files

jobs:
  collect:
    runs-on: ubuntu-latest
    # Healthy runs are ~8 min; per-step timeouts below keep any single phase
    # bounded. 25 min is the overall safety net — see header comment.
    timeout-minutes: 25
    env:
      # Force line-buffered stdout for every Python step so progress logs
      # appear in real time. The Apr-29 cancellation showed nothing between
      # collector start and kill — that was buffering, not a true hang in
      # the early scrapers.
      PYTHONUNBUFFERED: '1'

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Discover venues (Google Maps)
        # Non-destructive: discover_venues.py is wrapped so a missing
        # APIFY_TOKEN, an Apify HTTP error, or a top-level exception all
        # exit 0 and leave existing venue files alone. Each of those
        # paths now also fires a Sentry warning/exception, so failures
        # are visible without grep-diving the workflow log. We do NOT
        # `|| echo` here any more — the script already exits 0 on
        # expected failures, and masking exit codes would re-introduce
        # the double-silencing the audit flagged.
        #
        # Step-level timeout: the in-script Apify call uses a 120s read
        # timeout per category, and the script enforces an internal
        # ~220s total budget to bail with partial results before GitHub
        # forcibly kills us. 6 min is a hard outer ceiling.
        #
        # continue-on-error: discovery is a best-effort refresh of
        # venues.json. Run 25123919466 had this step killed at the 6m
        # cap and the workflow then skipped the collector — that's the
        # opposite of what we want. The collector has its own fallback
        # to the existing venues.json / facebook_venues.json, so even a
        # nonzero exit here (e.g. an unexpected SIGKILL on step timeout)
        # must not block the weekly pull. The script itself still exits
        # 0 on expected failures, so this only catches the truly
        # unexpected.
        timeout-minutes: 6
        continue-on-error: true
        env:
          APIFY_TOKEN:        ${{ secrets.APIFY_TOKEN }}
          SENTRY_DSN:         ${{ secrets.SENTRY_DSN }}
          SENTRY_ENVIRONMENT: production
        run: python -u discover_venues.py

      - name: Run event collector
        # Step-level timeout: collector chains several scrapers (city
        # calendar, library, theatre victoria, allevents, Apify FB events,
        # optionally FB/IG posts, Perplexity sonar discovery, AI review).
        # Each remote call has its own 240s ceiling. 15 min lets a couple
        # of slow remotes time out without killing the whole job — but
        # caps a stuck step so the workflow fails loudly instead of being
        # silently cancelled at the job-level timeout.
        timeout-minutes: 15
        env:
          OPENAI_API_KEY:     ${{ secrets.OPENAI_API_KEY }}
          PERPLEXITY_API_KEY: ${{ secrets.PERPLEXITY_API_KEY }}
          APIFY_TOKEN:        ${{ secrets.APIFY_TOKEN }}
          SENTRY_DSN:         ${{ secrets.SENTRY_DSN }}
          SENTRY_ENVIRONMENT: production
          # Toggle the FB-posts → sonar pipeline. Off until the Apify monthly
          # cap resets and we've verified post-volume cost. Set repository
          # variable FB_POSTS_ENABLED=1 (Settings → Variables → Actions) to
          # turn on without editing this file.
          FB_POSTS_ENABLED:   ${{ vars.FB_POSTS_ENABLED }}
          # Toggle the IG-posts → sonar pipeline (mirrors FB_POSTS_ENABLED).
          # Off until production-validated. Set repository variable
          # IG_POSTS_ENABLED=1 (Settings → Variables → Actions) to enable.
          IG_POSTS_ENABLED:   ${{ vars.IG_POSTS_ENABLED }}
        run: |
          # --candidates-only: write candidates.json but DO NOT overwrite
          # docs/events.json. The curated fallback at docs/events.json is
          # owned exclusively by the admin Save & Publish flow.
          # `python -u` is belt-and-braces with PYTHONUNBUFFERED at the
          # job env — leaves no room for a buffered-stdout regression to
          # hide a future stall.
          python -u collect_events.py \
            --output ./docs/events.json \
            --candidates ./candidates.json \
            --local-dir . \
            --days 14 \
            --candidates-only

      - name: Check for changes
        id: changes
        run: |
          # Note: docs/events.json is intentionally NOT in this list — the
          # weekly job runs in --candidates-only mode and must never commit
          # changes to the curated fallback.
          # collection_metadata.json is included so the admin Sources tab on
          # Railway can read per-source pull counts/timestamps after each run.
          if git diff --quiet candidates.json collection_metadata.json venues.json pending_venues.json facebook_venues.backup.json 2>/dev/null; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "The Vic 361 Bot"
          git config user.email "bot@thevic361.com"
          # Add venue files only if they exist (discovery may have been skipped).
          for f in venues.json pending_venues.json facebook_venues.backup.json; do
            [ -f "$f" ] && git add "$f"
          done
          # docs/events.json is deliberately omitted: it's the curated
          # fallback and only the admin Save & Publish flow (running with
          # GITHUB_TOKEN) is allowed to commit to it.
          git add candidates.json
          # collection_metadata.json is the admin Sources-tab feed (per-source
          # counts + timestamps). Add it only if the collector wrote one.
          [ -f collection_metadata.json ] && git add collection_metadata.json
          git commit -m "🏙️ Update candidates $(date +'%Y-%m-%d')"
          git push
